
# Cert Agent V3 — Step-by-Step تشغيل من الصفر (Runbook)

> هذا الملف مخصص للتشغيل العملي.
> افترض إنك **مش عارف أي حاجة** وعايز تمشي خطوة خطوة من غير ما تغلط.

---

## الجزء 1: المتطلبات الأساسية (تتعمل مرة واحدة على كل جهاز)

### 1️⃣ تثبيت Python
1. افتح المتصفح
2. ادخل على:
   https://www.python.org/downloads/release/python-31013/
3. نزّل **Python 3.10.x (64-bit)**
4. أثناء التثبيت:
   - ✔️ فعل خيار **Add Python to PATH**
   - ✔️ Install for all users
5. بعد التثبيت افتح Command Prompt واكتب:
   ```
   python --version
   ```
   لازم يطلع:
   ```
   Python 3.10.x
   ```

---

### 2️⃣ تثبيت Git
1. نزّل Git من:
   https://git-scm.com/download/win
2. اختار الإعدادات الافتراضية
3. افتح Command Prompt واكتب:
   ```
   git --version
   ```

---

## الجزء 2: تجهيز المشروع على أول جهاز (Device A)

### 3️⃣ نسخ المشروع
1. اختار مكان على الهارد (مثال: `F:\Projects`)
2. افتح Command Prompt في المكان ده
3. نفّذ:
   ```
   git clone <REPO_URL>
   cd cert-agent-v3
   ```

---

### 4️⃣ إنشاء Virtual Environment
داخل فولدر المشروع:
```
python -m venv .venv
```

تفعيل البيئة:
```
.venv\Scripts\activate
```

لو التفعيل نجح هتشوف:
```
(.venv)
```

---

### 5️⃣ تثبيت المكتبات
بعد تفعيل البيئة:
```
pip install -r requirements.txt
```

استنى لحد ما يخلص من غير Errors.

---

### 6️⃣ اختبار التشغيل الأول
نفّذ:
```
python Main.py
```

لازم يطلع:
```
Cert Agent V3 is running
```

---

## الجزء 3: تجهيز جهاز تاني (Device B, C, …)

كرر **نفس الخطوات بالترتيب**:

1. تثبيت Python 3.10
2. تثبيت Git
3. clone المشروع
4. إنشاء venv
5. تفعيل venv
6. pip install -r requirements.txt
7. تشغيل:
   ```
   python Main.py
   ```

> ❗ ممنوع تنسخ فولدر `.venv` من جهاز لجهاز

---

## الجزء 4: طريقة الشغل بين الأجهزة

### القاعدة الذهبية
- جهاز واحد بس تكتب عليه في نفس الوقت
- أي تعديل:
  ```
  git status
  git add .
  git commit -m "وصف التعديل"
  git push
  ```

على الجهاز التاني:
```
git pull
```

وبعدين:
```
.venv\Scripts\activate
python Main.py
```

---

## الجزء 5: تشغيل المشروع يوميًا

كل مرة تفتح الجهاز:

1. افتح Command Prompt
2. روح لفولدر المشروع
3. فعّل البيئة:
   ```
   .venv\Scripts\activate
   ```
4. شغّل:
   ```
   python Main.py
   ```

---

## الجزء 6: مشاكل شائعة وحلولها

### ❌ python مش معروف
- Python مش مضاف للـ PATH
- الحل: إعادة تثبيت Python مع تفعيل Add to PATH

---

### ❌ pip مش شغال
نفّذ:
```
python -m ensurepip --upgrade
```

---

### ❌ ImportError
- غالبًا نسيت `__init__.py`
- أو شغلت من غير تفعيل venv

---

### ❌ مكتبة ناقصة
- اتأكد إنك فعلت venv
- وبعدين:
```
pip install -r requirements.txt
```

---

## الجزء 7: ملاحظات مهمة جدًا

- ❌ متشغلش المشروع من غير venv
- ❌ متعدّليش في requirements.txt من غير commit
- ❌ متحطش ملفات CSV أو PDF في git
- ✔️ أي جهاز جديد = نفس الخطوات

---

## الخلاصة
لو مشيت على الملف ده حرفيًا:
- المشروع هيشتغل على أي جهاز
- مفيش خبط بيئة
- مفيش “كان شغال عندي”
- جاهز يتحول Service بعدين


الخيار 1️⃣ (الأسرع دلوقتي): فيديوهات موثوقة تغطي نفس الخطوات 1:1

دي فيديوهات عامة لكنها تطابق الـ Runbook اللي عملناه:

🔹 تثبيت Python + venv (Windows)

Corey Schafer – Python Virtual Environments
https://www.youtube.com/watch?v=Kg1Yvry_Ydk

🔹 Git + clone + pull/push

Traversy Media – Git & GitHub Crash Course
https://www.youtube.com/watch?v=SWYqp7iY_Tc

🔹 تشغيل Python مشروع من Command Prompt

Programming with Mosh – Python for Beginners (تشغيل المشاريع)
https://www.youtube.com/watch?v=_uQrJ0TkZlc

الثلاثة دول مع بعض = نفس اللي في الملف خطوة بخطوة.