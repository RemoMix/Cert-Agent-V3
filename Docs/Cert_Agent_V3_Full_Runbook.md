
# Cert Agent V3 — دليل التشغيل الكامل (Markdown)

> الدليل ده مكتوب علشان أي شخص (حتى لو مبتدئ تمامًا) يقدر:
> - يجهّز جهاز جديد
> - يشتغل على المشروع
> - يتنقل بين الأجهزة
> - من غير ما يلخبط الكود أو البيئة

---

## 1️⃣ تثبيت Python 3.10

### الخطوات
1. افتح المتصفح
2. ادخل على:
   https://www.python.org/downloads/release/python-31013/
3. نزّل **Python 3.10.x (64-bit)**
4. أثناء التثبيت:
   - ✔️ فعّل **Add Python to PATH**
   - ✔️ Install for all users
5. افتح Command Prompt أو PowerShell واكتب:
   ```
   python --version
   ```
   لازم يطلع:
   ```
   Python 3.10.x
   ```
## 2️⃣ تثبيت Git

### الخطوات
1. ادخل على:
   https://git-scm.com/download/win
2. نزّل **Git for Windows x64 Setup**
3. أثناء التثبيت:
   - اختار:
     **Git from the command line and also from 3rd-party software**
4. بعد التثبيت افتح PowerShell جديد واكتب:
   ```
   git --version
   ```
---

## 3️⃣ تثبيت VS Code

### الخطوات
1. ادخل على:
   https://code.visualstudio.com/
2. نزّل وثبّت VS Code
3. افتحه

### أهم أوامر / اختصارات VS Code
```
Ctrl + `        فتح Terminal
Ctrl + Shift P  فتح Command Palette
```

---

## 4️⃣ رفع المشروع على GitHub (أول مرة)

1. افتح GitHub Desktop
2. Add Local Repository → اختار فولدر المشروع
3. Publish Repository
4. اكتب اسم الريبو (مثال: cert-agent-v3)
5. Publish

---

## 5️⃣ تنزيل المشروع من GitHub (جهاز جديد)

1. افتح GitHub Desktop
2. File → Clone Repository
3. اختار المشروع
4. اختار مكانه على الهارد

---

## 6️⃣ فتح CMD / Terminal داخل VS Code

- افتح VS Code
- File → Open Folder → اختار فولدر المشروع
- اضغط:
  ```
  Ctrl + `
  ```

ده Terminal مربوط بالمشروع مباشرة.

---

## 7️⃣ إنشاء وتفعيل Virtual Environment (venv)

```cmd
python -m venv .venv
.venv\Scripts\activate
```

لو التفعيل نجح هتشوف:
```
(.venv)
```

---

## 8️⃣ تثبيت المكتبات

```cmd
pip install -r requirements.txt
```

---

## 9️⃣ تشغيل المشروع

```cmd
python Main.py
```

لازم تشوف رسالة إن المشروع شغال.

---

## 🔟 قبل ما تبدأ شغل على المشروع

دايمًا:
```cmd
git pull
.venv\Scripts\activate
python main.py   # تأكد إنه شغال
```

---

## 1️⃣1️⃣ بعد ما تخلص شغل اليوم

```cmd
git status
git add .
git commit -m "وصف التعديل"
git push
```

---

## 1️⃣2️⃣ بعد ما المشروع يشتغل كويس (إصدار مستقر)

- اعمل:
  ```cmd
  git commit -m "Stable version"
  git push
  ```
- خُد Tag (اختياري):
  ```cmd
  git tag v3.0
  git push origin v3.0
  ```

---

## 1️⃣3️⃣ أهم أوامر CMD واستخدامها

```cmd
cd folder        # الدخول لفولدر
dir              # عرض الملفات
cls              # مسح الشاشة
where python     # مكان بايثون
where git        # مكان git
pip list         # المكتبات المثبتة
```

---
### أهم أوامر Python
```cmd
python --version        # معرفة نسخة بايثون
python file.py          # تشغيل ملف بايثون
python -m venv .venv    # إنشاء virtual environment
```

---
### أهم أوامر Git
```cmd
git --version           # التأكد من تثبيت git
git init                # تهيئة مشروع git
git status              # حالة الملفات
git add .               # تجهيز كل التعديلات
git commit -m "message" # حفظ نسخة
git push                # رفع التعديلات
git pull                # تنزيل آخر نسخة
git clone URL           # تنزيل مشروع من GitHub
```

## الخلاصة

لو مشيت على الملف ده:
- أي جهاز جديد يشتغل
- مفيش خبط بيئة
- شغل منظم
- جاهز Service بعدين

> الملف ده معمول بصيغة Markdown علشان تقدر تعدّل وتزوّد فيه وقت ما تحب.
